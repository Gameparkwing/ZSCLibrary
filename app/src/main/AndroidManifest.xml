<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.waxberry.zsclibrary">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.BookInformationActivity"
            android:label="@string/title_activity_book_information" >
        </activity>
        <activity
            android:name=".activity.ReaderServiceActivity"
            android:label="@string/title_activity_reader_service" >
        </activity>
    </application>
</manifest>
